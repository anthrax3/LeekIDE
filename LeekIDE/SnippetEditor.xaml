<Window x:Class="LeekIDE.SnippetEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeekIDE"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        mc:Ignorable="d"
        Title="SnippetEditor" Height="302" Width="499" d:DataContext="{d:DesignInstance local:SnippetEditor}">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Left" Orientation="Vertical">
                <Button Content="Add" Click="ButtonBase_OnClick"/>
                <Button Content="Delete" IsEnabled="False" Click="WhenDeleting" Name="DeleteButton"/>
                <ListView ItemsSource="{Binding CodeSnippets}" SelectionChanged="Selector_OnSelected" Name="View">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="75" DisplayMemberBinding="{Binding ShortenedCalling}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

            </StackPanel>
            <Grid DockPanel.Dock="Right">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20*"/>
                    <RowDefinition Height="251*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Shortened name :" FontSize="8"/>
                    <TextBox MinWidth="80" Margin="1" Text="{Binding SelectedItem.(local:CodeSnippet.ShortenedCalling), ElementName=View}"/>
                </StackPanel>
                <avalonedit:TextEditor
    Name="textEditor"
    FontFamily="Consolas"
    SyntaxHighlighting="LeekScript"
    FontSize="{Binding Value, ElementName=Size}"
    IsEnabled="False" Grid.Row="1"          
    />
            </Grid>

        </DockPanel>
    </Grid>
</Window>
